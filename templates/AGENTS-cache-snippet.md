## Prompt Cache

- 高频请求按“固定规则 -> 固定输出格式 -> 动态参数”的顺序组装。
- 固定前缀必须保持稳定；不要改动其顺序、措辞、标点或空行。
- 用户请求、文件内容、日期、随机 ID、git 状态和任务进度只能位于末尾的动态参数区。
- 长资料按需读取，优先使用路径、标题、锚点和摘要；不要预加载无关目录、示例或历史内容。
- 高频子代理必须复用项目 `prompts/` 中的同一模板，变量统一放在末尾的“参数”区。
- 修改模板后，记录并比较输入 token、缓存读取 token、延迟和费用。

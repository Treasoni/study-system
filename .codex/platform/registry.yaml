apiVersion: agents.study-system/v1
discovery:
  kindDirectories:
    Workflow: .codex/workflows
    Skill: .codex/skills
    Subagent: .codex/agents
    Hook: .codex/hooks
policy:
  allowedPermissions:
    filesystem:
      - none
      - read
      - write
    network:
      - none
      - allow
    subprocess:
      - none
      - allow
    git:
      - none
      - read
      - write
  hookEvents:
    - SessionStart
    - Stop

# 学习笔记自动化工作流 —— 设计文档

> 基于 Claude Code 的资料收集 → 整理 → 写作 → 美化全流程方案
> 
> 版本：v1.0 | 状态：设计阶段，待验证

---

## 一、项目背景与目标

### 1.1 要解决的问题

设计一个基于 Claude Code 的自动化工作流，用于高效产出学习笔记（实战笔记、概念笔记、学习心得等），覆盖从资料收集到最终美化的全流程。

### 1.2 核心目标

| 目标 | 说明 |
|------|------|
| **高效收集** | 多源并行搜索，token 消耗可控 |
| **结构化整理** | 去重、归类、提炼核心观点 |
| **高质量写作** | 根据笔记类型自适应风格 |
| **灵活调整** | 支持中途改变方向，渐进式探索 |
| **美观输出** | 支持 Markdown / PDF / Word / PPT |

---

## 二、整体架构

### 2.1 四阶段流程

```
┌─────────────────────────────────────────────────────────┐
│                    编排层 (主 Agent)                      │
│  控制流程、传递上下文、做决策、质量把关                      │
├──────────┬──────────────┬──────────────┬────────────────┤
│  ① 收集   │  ② 整理分析   │  ③ 撰写文章   │  ④ 美化输出     │
│ 多源并行  │ 结构化处理    │ 分章节生成    │ 排版/图表/      │
│ 搜索+抓取 │ 去重+提炼    │ 引用+论证     │ 多格式导出      │
└──────────┴──────────────┴──────────────┴────────────────┘
```

### 2.2 两模式支持

| 模式 | 适合场景 | 体验 |
|------|---------|------|
| **大纲模式** | 结构化学习、有明确目标 | 按章节推进，产出体系化笔记 |
| **随性模式** | 兴趣驱动、探索式学习 | 边聊边记，事后拼装成文 |

---

## 三、Skill vs Subagent 选型决策

### 3.1 判断标准

| 维度 | Skill | Subagent (Fork) |
|------|-------|-----------------|
| 上下文大小 | 共享主对话上下文 | 独立上下文，不占主窗口 |
| Token 消耗 | 消耗主对话 token | 消耗独立 token（隔离） |
| 结果质量 | 主 Agent 直接感知 | 返回摘要，需验证 |
| 适用场景 | 需要精细控制、反复迭代 | 独立子任务、可并行 |

### 3.2 各阶段分配

| 阶段 | 执行方式 | 理由 |
|------|---------|------|
| ① 资料收集 | **Fork Subagent × 2-5（并行）** | 搜索/抓取产生大量 token，必须隔离 |
| ② 整理分析 | **subAgent 执行** | 需要精细判断素材质量和关联性 |
| ③ 撰写文章 | **subAgent 执行** | 需要用户对质量、风格、论点的精细控制 |
| ④ 美化输出 | **Skill** | 现成封装，开箱即用 |

### 3.3 固定 Agent vs 临时 Prompt

| 方式 | 适用场景 |
|------|---------|
| 临时 prompt | 探索阶段、一次性任务、灵活调整 |
| `agents/` 文件夹固化 | 高频复用、团队共享、精调迭代 |

**建议路径**：先临时跑 2-3 次 → 摸清习惯 → 提炼为固定 agent 模板。

---

## 四、Token 节省策略

### 4.1 核心原则

资料收集是 token 黑洞，必须隔离处理。

### 4.2 四大策略

| 策略 | 做法 |
|------|------|
| **Fork 隔离** | 收集 subagent 用 fork 模式，独立上下文，不污染主对话 |
| **强制摘要** | subagent 只返回结构化摘要（每条 ≤200 字），不返回原文 |
| **两阶段收集** | 先粗筛（标题+一句话摘要）→ 用户选方向 → 再精读（定向深挖） |
| **文件落盘** | 收集结果写入 `.md` 文件，主 Agent 按需 Read，不常驻上下文 |

---

## 五、各环节详细设计

### 环节 0：意图澄清

**执行者**：主 Agent

**目的**：用最少的问题锁定学习方向，支持模糊输入。

**逻辑**：
- 用户输入明确（有主题 + 笔记类型 + 深度）→ 直接进入环节 1
- 用户输入模糊（只有主题）→ 先探测再让用户选
- 用户输入极模糊（只有方向）→ 探测后展示全景菜单

**产物**：`/workspace/learning_notes/00_intent.md`

```
## 学习意图
- 主题：xxx
- 类型：实战笔记 / 概念笔记 / 心得笔记 / 对比笔记
- 深度：入门了解 / 能上手做 / 能讲给别人听
- 基础：零基础 / 有一定了解 / 比较熟悉
- 特别关注：xxx（如有）
```

---

### 环节 1：探测式收集

**执行者**：Fork Subagent × 2-3（并行）

**目的**：用最小 token 成本摸清资料全貌，帮用户看到可选方向。

**Subagent Prompt 模板**：

```
你是资料探测员。你的任务是对一个学习主题做快速扫描，只返回概览，不深入细节。

学习主题：{topic}
扫描角度：{angle}  （例如：实战教程 / 概念原理 / 对比分析 / 工具生态）

步骤：
1. 用 WebSearch 搜索 2-3 个相关关键词
2. 对排名靠前的结果，用 WebFetch 快速浏览
3. 返回以下格式，严格遵守字数限制：

## 扫描结果：{angle}

### 方向一：[用一句话描述这个学习方向]
- 代表资源（只列标题+链接，不超过3个）
- 适合人群：（≤20字）
- 预计产出：（≤30字，学完后能做什么）

### 方向二：同上
### 方向三：同上

## 总结建议（≤50字）
这个角度下，最适合哪类学习者，最大收获是什么。

规则：
- 每个方向总字数不超过 150 字
- 不复制原文，只提炼要点
- 不返回广告、导航、无关内容
- 如果搜索结果质量差，诚实说明
```

**主 Agent 汇总展示**：

```
根据探测结果，"{topic}" 的学习大致分为以下方向：

1. {方向A} — {一句话描述}
2. {方向B} — {一句话描述}  
3. {方向C} — {一句话描述}

你更想深入哪个方向？
```

---

### 环节 2：深度收集

**执行者**：Fork Subagent × 2-3（并行）

**目的**：在用户选定的方向上，收集高质量、结构化的学习素材。

**Subagent Prompt 模板**：

```
你是深度资料收集员。用户已选定了学习方向，你需要收集高质量、结构化的学习素材。

学习主题：{topic}
选定方向：{direction}
目标深度：{depth}  （入门/上手/精通）
用户基础：{level}  （零基础/有了解/熟悉）

收集要求：
1. 覆盖以下维度（按需分配，每个 subagent 负责 1-2 个维度）：
   - 核心概念/理论
   - 实战代码/项目案例
   - 常见坑/最佳实践
   - 工具链/生态
   - 进阶路径/学习资源

2. 用 WebSearch + WebFetch 收集，每个维度找 3-5 条高质量资料

3. 返回格式（严格遵守）：

## 维度：{维度名}

### 资料 1：[标题]
- 来源：{URL}
- 类型：官方文档/教程/博客/视频/项目
- 核心内容：（≤100字概括）
- 关键代码/概念：（如有，摘录最核心的 ≤50 字）
- 难度：入门/中级/高级
- 推荐理由：（≤30字）

（重复，每个维度 3-5 条）

规则：
- 优先官方文档和高质量教程
- 每条资料总字数不超过 200 字
- 标注难度，方便后续按用户基础筛选
- 如果某个维度资料太少，诚实说明并建议替代方向
```

**产物**：`/workspace/learning_notes/02_deep_research.md`

**主 Agent 确认话术**：

```
深度收集完成，共收集 {N} 条资料，覆盖 {维度列表}。

资料质量概况：
- 官方文档：{n} 篇
- 实战教程：{n} 篇
- 深度文章：{n} 篇

这些素材够了吗？有没有想补充的角度？
```

---

### 环节 3：大纲生成（大纲模式专用）

**执行者**：subagent

**目的**：基于收集的素材，生成学习路径大纲，让用户确认。

> ⚠️ 此环节仅在「大纲模式」下执行。随性模式跳过此环节，直接进入自由探索写作。

**主 Agent Prompt**：

```
你负责基于收集的素材生成学习笔记大纲。

输入：/workspace/learning_notes/02_deep_research.md
参考：/workspace/learning_notes/00_intent.md

生成大纲要求：
1. 根据笔记类型调整结构：
   - 实战笔记：按项目推进顺序（环境搭建→核心功能→进阶优化→部署）
   - 概念笔记：按理解层次（概览→核心概念→深入原理→延伸）
   - 心得笔记：按学习历程（初识→踩坑→顿悟→总结）
   - 对比笔记：按对比维度（分别介绍→逐项对比→选型建议）

2. 每章标注：预计篇幅（短/中/长）、依赖的素材编号、是否有代码示例

3. 大纲层级不超过 3 级

4. 生成后写入 /workspace/learning_notes/03_outline.md
5. 展示给用户并询问：「大纲顺序和深度合适吗？有没有想调整的？」
```

**产物**：`/workspace/learning_notes/03_outline.md`

---

### 环节 4：写作

**执行者**：subagent

**目的**：按大纲或按用户兴趣逐段生成笔记内容。

#### 4a. 大纲模式 —— 逐章写作

```
你负责按大纲逐章撰写学习笔记。

当前章节：第 {N} 章《{章节名}》
大纲参考：/workspace/learning_notes/03_outline.md
素材参考：/workspace/learning_notes/02_deep_research.md
用户基础：{level}
笔记类型：{type}

写作要求：
1. 根据笔记类型调整风格：
   - 实战笔记：步骤清晰、代码完整可运行、解释每一步为什么
   - 概念笔记：先直觉理解再严谨定义、配类比/图示、避免纯理论堆砌
   - 心得笔记：第一人称、记录思考过程、踩坑和解决过程
   - 对比笔记：公平呈现各方、用表格对比、给出选型场景

2. 结构：开头（解决什么问题）→ 主体（按要点展开）→ 结尾（小结+预告）

3. 代码示例（如有）：完整可运行，关键行加注释

4. 引用来源用脚注格式：[来源](URL)

5. 写完后保存到 /workspace/learning_notes/chapters/{N}_{章节名}.md
6. 展示给用户并询问：「这章满意吗？继续下一章，还是想调整方向？」
```

#### 4b. 随性模式 —— 自由探索写作

```
你是学习笔记助手，用户想自由探索一个主题。

可用素材：/workspace/learning_notes/02_deep_research.md
用户基础：{level}
笔记类型：{type}

对话流程：

1. 开场：
   「素材都准备好了。关于 {topic}，你想先从哪个话题开始？」
   列出 3-5 个可从素材中提取的入口话题供选择。

2. 用户选一个 → 围绕该话题写一段笔记（300-800字），展示。

3. 追问：
   「这个话题还想继续深入吗？还是换个话题？」
   
   用户可能的回答及处理：
   - "继续深入" → 深挖当前话题，补充更多细节或代码示例
   - "换一个" → 从素材池中提供新的话题选项
   - "我对 XX 突然感兴趣了" → 追加搜索 XX，然后围绕 XX 写
   - "差不多了" → 进入收尾组装

4. 每段笔记即时保存到 /workspace/learning_notes/chapters/ 下
```

---

### 环节 5：收尾组装

**执行者**：主 Agent

**目的**：将所有零散片段拼装为最终笔记。

```
你是笔记整理助手。用户已完成自由探索，现在需要收尾。

已完成的话题片段：
{topic_list}

询问用户：
「今天聊了这些话题。要我帮你：
 A) 按当前顺序拼成一篇随笔笔记
 B) 重新排序整理成结构化笔记
 C) 就这样，保持零散片段」

根据选择：
- A → 按时间顺序拼接，添加过渡语，生成连贯文章
- B → 重新归类排序，生成目录，补充缺失的衔接
- C → 保持原样，仅做格式统一
```

---

### 环节 6：美化输出

**执行者**：主 Agent 调用 Skill

**目的**：将 Markdown 合成为最终文档。

```
1. 合并所有章节文件为一个完整 Markdown
2. 添加目录、标题层级统一、引用检查
3. 根据用户选择的输出格式调用对应 skill：
   - Markdown → 直接输出
   - PDF → 调用 pdf skill
   - Word → 调用 docx skill
   - PPT → 调用 pptx skill
4. 最终产物放到 /workspace/learning_notes/output/
```

---

## 六、渐进式调整机制

### 6.1 检查点设计

每个阶段之间设置显式确认点，不一路跑到黑：

```
环节 0 → [确认] → 环节 1 → [确认] → 环节 2 → [确认] → 环节 3/4 → [每章确认] → 环节 5
```

### 6.2 用户反馈分类器

主 Agent 在每次用户反馈后执行判断：

| 用户反馈 | 分类 | 处理动作 |
|---------|------|---------|
| "继续" / "OK" / "没问题" | 正常推进 | 进入下一阶段 |
| "XX 不够详细" / "补充 XX" | 追加需求 | 在当前阶段追加，不回退 |
| "我想换个方向" / "对 XX 更感兴趣" | 方向调整 | 记录新方向 → 判断素材是否已覆盖 → 未覆盖则追加收集 |
| "从头来吧" / "换个主题" | 重启 | 保留已有素材文件，回到环节 0 |
| 不确定意图 | 追问 | 追问一个具体选择题，不给开放性问题 |

### 6.3 回退成本控制

| 回退场景 | 成本 | 处理方式 |
|----------|------|----------|
| 大纲调整 | 低 | 直接改大纲文件，已写章节不动 |
| 补充某方向素材 | 中 | 只追加搜索该方向，不动已有素材 |
| 推翻重来 | 高 | 保留原始素材，重新生成大纲 |
| 发现新兴趣点 | 低 | 追加一次探测收集，合并到素材池 |

---

## 七、文件结构

```
/workspace/learning_notes/
├── 00_intent.md              ← 学习意图（可被后续调整更新）
├── 01_explore_result.md      ← 探测收集结果
├── 02_deep_research.md       ← 深度素材（可增量追加）
├── 03_outline.md             ← 大纲（仅大纲模式，可调整）
├── chapters/
│   ├── 01_xxx.md
│   ├── 02_xxx.md
│   └── ...
├── output/
│   └── final_note.md/pdf     ← 最终产物
└── .workflow_state.json      ← 当前阶段状态
```

---

## 八、Prompt 体系总览

| 环节 | 执行者 | Token 风险 | 关键约束 |
|------|--------|-----------|---------|
| 0. 意图澄清 | 主 Agent | 低 | 不问超过 3 个问题 |
| 1. 探测收集 | Fork Subagent × 2-3 | 低（隔离） | 每条 ≤150 字 |
| 2. 深度收集 | Fork Subagent × 2-3 | 中（隔离） | 每条 ≤200 字，标注难度 |
| 3. 大纲生成 | 主 Agent | 低 | 仅大纲模式执行 |
| 4. 写作 | 主 Agent（循环） | 中 | 每章/每段停下来确认 |
| 5. 收尾组装 | 主 Agent | 低 | 提供 A/B/C 选项 |
| 6. 美化输出 | Skill | 低 | 调用现成 skill |

---

## 九、后续计划

### 9.1 待验证项

- [ ] Fork subagent 实际 token 消耗和资料质量
- [ ] 收集→写作的衔接流畅度
- [ ] 随性模式的用户体验
- [ ] 中途调整方向的实际成本

### 9.2 待建设项

- [ ] 固化 `research-collector` agent 到 `agents/` 文件夹
- [ ] 固化 `learning-note-orchestrator` agent 模板
- [ ] 建立常用学习主题的信源库

### 9.3 决策待定

- [ ] 固定 agent 放在 `agents/` 文件夹 vs 临时 prompt 调用 → 建议先跑通 2-3 次再固化

---

## 附录：关键设计原则

1. **阶段间设检查点**：每完成一个阶段就停下来让用户确认
2. **中间产物文件化**：每个阶段的输出落盘，回退时增量追加
3. **收集必须隔离**：subagent fork 模式，强制字数限制
4. **大纲可选**：支持大纲模式和随性模式自由切换
5. **不替用户决定，帮用户看清选项**：探测式收集展示全景，让用户选择

---

> 文档维护者：产品需求全流程协同助手
> 
> 创建日期：2025-07-17
